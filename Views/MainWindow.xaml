<Window x:Class="PrviProjekat.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{DynamicResource App_Title}"
        Height="650" Width="1100"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource BackgroundBrush}"
        FontFamily="{DynamicResource MainFont}"
        Loaded="Window_Loaded">

    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="260"/>
            <!-- LIJEVI MENI -->
            <ColumnDefinition Width="*"/>
            <!-- GLAVNI SADRŽAJ -->
        </Grid.ColumnDefinitions>

        <!-- ====================== LIJEVI VERTIKALNI MENI ====================== -->
        <Border Background="{DynamicResource PanelBackground}"
                CornerRadius="16"
                BorderBrush="#D0D0D0"
                BorderThickness="1"
                Padding="0,20">
            <StackPanel>

                <!-- Logo -->
                <TextBlock Text="Cvjećara Manager"
                           FontSize="20"
                           FontWeight="Bold"
                           Foreground="{DynamicResource AccentBrush}"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,40"/>

                <!-- Narudžbe -->
                <Button x:Name="BtnOrders" Click="PregledNarudzbi_Click"
                        Height="56" Margin="16,8"
                        Background="Transparent"
                        BorderThickness="0"
                        Cursor="Hand">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="12" Padding="16">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="&#xE8B7;" FontFamily="Segoe MDL2 Assets" FontSize="22" Margin="0,0,16,0"/>
                                    <TextBlock Text="{DynamicResource Menu_Orders}" FontWeight="Medium" FontSize="15"/>
                                </StackPanel>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="border" Property="Background" Value="#18000000"/>
                                </Trigger>
                                <DataTrigger Binding="{Binding ElementName=OrdersPanel, Path=Visibility}" Value="Visible">
                                    <Setter TargetName="border" Property="Background" Value="{DynamicResource SuccessBrush}"/>
                                    <Setter Property="Foreground" Value="White"/>
                                </DataTrigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>

                <!-- Proizvodi -->
                <Button x:Name="BtnProducts" Click="Proizvodi_Click"
                        Height="56" Margin="16,8"
                        Background="Transparent"
                        BorderThickness="0"
                        Cursor="Hand">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="12" Padding="16">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="&#xE7C1;" FontFamily="Segoe MDL2 Assets" FontSize="22" Margin="0,0,16,0"/>
                                    <TextBlock Text="{DynamicResource Menu_Products}" FontWeight="Medium" FontSize="15"/>
                                </StackPanel>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="border" Property="Background" Value="#18000000"/>
                                </Trigger>
                                <DataTrigger Binding="{Binding ElementName=ProductsPanel, Path=Visibility}" Value="Visible">
                                    <Setter TargetName="border" Property="Background" Value="{DynamicResource AccentBrush}"/>
                                    <Setter Property="Foreground" Value="White"/>
                                </DataTrigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>

                <!-- Zaposleni -->
                <Button x:Name="BtnEmployees" Click="Zaposleni_Click"
                        Height="56" Margin="16,8"
                        Background="Transparent"
                        BorderThickness="0"
                        Cursor="Hand">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="12" Padding="16">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="&#xE716;" FontFamily="Segoe MDL2 Assets" FontSize="22" Margin="0,0,16,0"/>
                                    <TextBlock Text="{DynamicResource Menu_Employees}" FontWeight="Medium" FontSize="15"/>
                                </StackPanel>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="border" Property="Background" Value="#18000000"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>

                <!-- Separator -->
                <Separator Margin="20,40" Background="#E0E0E0" Height="1"/>

                <!-- Odjava -->
                <Button Click="Odjava_Click"
                        Height="56" Margin="16,8"
                        Background="Transparent"
                        BorderThickness="0"
                        Cursor="Hand">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="12" Padding="16">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="&#xE7E8;" FontFamily="Segoe MDL2 Assets" FontSize="22" Margin="0,0,16,0" Foreground="{DynamicResource DangerBrush}"/>
                                    <TextBlock Text="{DynamicResource Btn_Logout}" FontWeight="Medium" FontSize="15" Foreground="{DynamicResource DangerBrush}"/>
                                </StackPanel>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="border" Property="Background" Value="#18FF0000"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>

            </StackPanel>
        </Border>

        <!-- ====================== DESNO: GLAVNI SADRŽAJ ====================== -->
        <Grid Grid.Column="1" Margin="30,0,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <!-- Teme + jezik -->
                <RowDefinition Height="*"/>
                <!-- Paneli -->
            </Grid.RowDefinitions>

            <!-- Teme + jezik u gornjem desnom uglu -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,10,0,0">
                <!-- Teme -->
                <StackPanel Orientation="Horizontal" Margin="0,0,20,0">
                    <RadioButton x:Name="LightRadioButton" GroupName="ThemeGroup" Tag="Light"
                                 Margin="6,0" ToolTip="{DynamicResource Theme_Light}"
                                 Click="ThemeRadioButton_Click">
                        <RadioButton.Template>
                            <ControlTemplate TargetType="RadioButton">
                                <Border Width="22" Height="22" CornerRadius="11" Background="White"
                                        BorderBrush="#CCCCCC" BorderThickness="2">
                                    <Border x:Name="inner" Width="10" Height="10" CornerRadius="5" Background="Transparent"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsChecked" Value="True">
                                        <Setter TargetName="inner" Property="Background" Value="#333"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </RadioButton.Template>
                    </RadioButton>
                    <RadioButton x:Name="PinkRadioButton" GroupName="ThemeGroup" Tag="Pink"
                                 Margin="6,0" ToolTip="{DynamicResource Theme_Pink}"
                                 Click="ThemeRadioButton_Click">
                        <RadioButton.Template>
                            <ControlTemplate TargetType="RadioButton">
                                <Border Width="22" Height="22" CornerRadius="11" Background="#EC4899"
                                        BorderBrush="#EC4899" BorderThickness="2">
                                    <Border x:Name="inner" Width="10" Height="10" CornerRadius="5" Background="Transparent"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsChecked" Value="True">
                                        <Setter TargetName="inner" Property="Background" Value="White"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </RadioButton.Template>
                    </RadioButton>
                    <RadioButton x:Name="OceanRadioButton" GroupName="ThemeGroup" Tag="Ocean"
                                 Margin="6,0" ToolTip="{DynamicResource Theme_Ocean}"
                                 Click="ThemeRadioButton_Click">
                        <RadioButton.Template>
                            <ControlTemplate TargetType="RadioButton">
                                <Border Width="22" Height="22" CornerRadius="11" Background="#0EA5E9"
                                        BorderBrush="#0EA5E9" BorderThickness="2">
                                    <Border x:Name="inner" Width="10" Height="10" CornerRadius="5" Background="Transparent"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsChecked" Value="True">
                                        <Setter TargetName="inner" Property="Background" Value="White"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </RadioButton.Template>
                    </RadioButton>
                </StackPanel>

                <!-- Jezik -->
                <StackPanel Orientation="Horizontal">
                    <Button Content="BS" Width="40" Height="40" Margin="4"
                            Background="{DynamicResource PanelBackground}"
                            Foreground="{DynamicResource TextBrush}"
                            FontWeight="Bold" FontSize="13"
                            Click="Lang_BS_Click" ToolTip="Bosanski">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" CornerRadius="10" Padding="8">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                    <Button Content="EN" Width="40" Height="40" Margin="4"
                            Background="{DynamicResource PanelBackground}"
                            Foreground="{DynamicResource TextBrush}"
                            FontWeight="Bold" FontSize="13"
                            Click="Lang_EN_Click" ToolTip="English">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" CornerRadius="10" Padding="8">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </StackPanel>
            </StackPanel>

            <!-- ====================== PANELI ====================== -->
            <!-- Narudžbe -->
            <Grid x:Name="OrdersPanel" Grid.Row="1" Margin="0,20,0,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Filteri -->
                <StackPanel Orientation="Horizontal" Margin="0,0,0,18">
                    <!-- U OBRADI -->
                    <Button x:Name="BtnPendingMain" 
        Content="{DynamicResource Status_Pending}" 
        Click="Obradi_Click"
        Width="110" Height="40" Margin="0,0,10,0"
        Background="#F39C12" Foreground="White" FontWeight="Medium"
        Cursor="Hand">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="border"
                    Background="#F39C12"
                    CornerRadius="10"
                    BorderBrush="#2C3E50"
                    BorderThickness="3">
                                    <!-- AKTIVNO: debela tamna ivica -->
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="border" Property="Background" Value="#E67E22"/>
                                    </Trigger>
                                    <Trigger Property="IsPressed" Value="True">
                                        <Setter TargetName="border" Property="Background" Value="#D35400"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>

                    <!-- ZAVRŠENO -->
                    <Button x:Name="BtnCompletedMain" 
        Content="{DynamicResource Status_Completed}" 
        Click="Zavrsene_Click"
        Width="110" Height="40" Margin="0,0,10,0"
        Background="{DynamicResource SuccessBrush}" Foreground="White" FontWeight="Medium"
        Cursor="Hand">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="border"
                    Background="{TemplateBinding Background}"
                    CornerRadius="10"
                    BorderBrush="{TemplateBinding Background}"
                    BorderThickness="2">
                                    <!-- NEAKTIVNO: ivica iste boje kao pozadina (nevidljiva) -->
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="border" Property="Background" Value="{DynamicResource SuccessHover}"/>
                                    </Trigger>
                                    <Trigger Property="IsPressed" Value="True">
                                        <Setter TargetName="border" Property="Background" Value="#1E8449"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>

                    <!-- OTKAZANO -->
                    <Button x:Name="BtnCanceledMain" 
        Content="{DynamicResource Status_Canceled}" 
        Click="Otkazane_Click"
        Width="110" Height="40" Margin="0,0,20,0"
        Background="{DynamicResource DangerBrush}" Foreground="White" FontWeight="Medium"
        Cursor="Hand">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="border"
                    Background="{TemplateBinding Background}"
                    CornerRadius="10"
                    BorderBrush="{TemplateBinding Background}"
                    BorderThickness="2">
                                    <!-- NEAKTIVNO -->
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="border" Property="Background" Value="{DynamicResource DangerHover}"/>
                                    </Trigger>
                                    <Trigger Property="IsPressed" Value="True">
                                        <Setter TargetName="border" Property="Background" Value="#C0392B"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                  
                    <ComboBox x:Name="PeriodComboBox"
                              Width="220" Height="40"
                              FontSize="13" SelectedIndex="0"
                              SelectionChanged="PeriodComboBox_SelectionChanged"
                              BorderBrush="#CCCCCC" BorderThickness="1"
                              Background="{DynamicResource PanelBackground}">
                        <ComboBoxItem Content="{DynamicResource Period_7Days}" Tag="7" IsSelected="True"/>
                        <ComboBoxItem Content="{DynamicResource Period_14Days}" Tag="14"/>
                        <ComboBoxItem Content="{DynamicResource Period_30Days}" Tag="30"/>
                        <ComboBoxItem Content="{DynamicResource Period_6Months}" Tag="180"/>
                        <ComboBoxItem Content="{DynamicResource Period_1Year}" Tag="365"/>
                        <Separator/>
                        <ComboBoxItem Content="{DynamicResource Period_All}" Tag="all" FontWeight="Bold"/>
                    </ComboBox>
                </StackPanel>

                <!-- DataGrid -->
                <!-- LIJEPI DATAGRID -->
                <!-- ==================== LIJEPI DATAGRID SA SCROLOM I SJENOM ==================== -->
                <Border Grid.Row="1" 
                        Background="White" 
                        CornerRadius="20" 
                        Margin="0,0,0,12"
                        BorderBrush="#E2E8F0"
                        BorderThickness="1.5">
                    <Border.Effect>
                        <DropShadowEffect Color="#000000" Direction="270" ShadowDepth="3" BlurRadius="12" Opacity="0.12"/>
                    </Border.Effect>

                    <ScrollViewer VerticalScrollBarVisibility="Auto" 
                                  HorizontalScrollBarVisibility="Disabled"
                                  Padding="8">

                        <DataGrid x:Name="OrdersGrid"
                                  AutoGenerateColumns="False"
                                  SelectionMode="Single"
                                  SelectionUnit="FullRow"
                                  IsReadOnly="True"
                                  Background="Transparent"
                                  BorderThickness="0"
                                  RowHeaderWidth="0"
                                  HeadersVisibility="Column"
                                  GridLinesVisibility="None"
                                  CanUserResizeColumns="True"
                                  CanUserReorderColumns="False"
                                  RowHeight="62"
                                  FontSize="13.5"
                                  MouseDoubleClick="OrdersGrid_MouseDoubleClick"
                                  MouseRightButtonUp="OrdersGrid_MouseRightButtonUp">

                            <!-- Stil reda -->
                            <DataGrid.RowStyle>
                                <Style TargetType="DataGridRow">
                                    <Setter Property="Background" Value="Transparent"/>
                                    <Setter Property="Cursor" Value="Hand"/>
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#F8FAFC"/>
                                        </Trigger>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter Property="Background" Value="#EBF8FF"/>
                                            <Setter Property="BorderThickness" Value="0,0,0,3"/>
                                            <Setter Property="BorderBrush" Value="#0EA5E9"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </DataGrid.RowStyle>

                            <!-- Stil ćelije -->
                            <DataGrid.CellStyle>
                                <Style TargetType="DataGridCell">
                                    <Setter Property="Padding" Value="18,0"/>
                                    <Setter Property="BorderThickness" Value="0"/>
                                    <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                                    <Style.Triggers>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter Property="Background" Value="Transparent"/>
                                            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </DataGrid.CellStyle>

                            <!-- Zaglavlje -->
                            <DataGrid.ColumnHeaderStyle>
                                <Style TargetType="DataGridColumnHeader">
                                    <Setter Property="Background" Value="#F1F5F9"/>
                                    <Setter Property="Foreground" Value="#475569"/>
                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                    <Setter Property="FontSize" Value="13"/>
                                    <Setter Property="Padding" Value="18,16"/>
                                    <Setter Property="Height" Value="52"/>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="DataGridColumnHeader">
                                                <Border Background="{TemplateBinding Background}" 
                                                        Padding="{TemplateBinding Padding}"
                                                        CornerRadius="12">
                                                    <ContentPresenter VerticalAlignment="Center"/>
                                                </Border>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </DataGrid.ColumnHeaderStyle>

                            <!-- Kolone -->
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="70">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Foreground" Value="#64748B"/>
                                            <Setter Property="FontWeight" Value="Medium"/>
                                            <Setter Property="HorizontalAlignment" Value="Center"/>
                                            <Setter Property="VerticalAlignment" Value="Center"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>

                                <DataGridTextColumn Header="{DynamicResource Col_Customer}" Binding="{Binding ImeKupca}" Width="190">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="FontWeight" Value="SemiBold"/>
                                            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
                                            <Setter Property="VerticalAlignment" Value="Center"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>

                                <DataGridTextColumn Header="{DynamicResource Col_Date}" Binding="{Binding Datum, StringFormat=dd.MM.yyyy HH:mm}" Width="140">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Foreground" Value="#64748B"/>
                                            <Setter Property="VerticalAlignment" Value="Center"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>

                                <DataGridTextColumn Header="{DynamicResource Col_Status}" Binding="{Binding Status}" Width="110">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="HorizontalAlignment" Value="Center"/>
                                            <Setter Property="FontWeight" Value="SemiBold"/>
                                            <Setter Property="VerticalAlignment" Value="Center"/>
                                            <Setter Property="Foreground" Value="{DynamicResource SuccessBrush}"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Status}" Value="Otkazano">
                                                    <Setter Property="Foreground" Value="{DynamicResource DangerBrush}"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Status}" Value="U obradi">
                                                    <Setter Property="Foreground" Value="#F59E0B"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>

                                <DataGridTextColumn Header="{DynamicResource Col_Total}" Binding="{Binding Ukupno, StringFormat={}{0:F2} KM}" Width="120">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="FontWeight" Value="Bold"/>
                                            <Setter Property="Foreground" Value="{DynamicResource SuccessBrush}"/>
                                            <Setter Property="HorizontalAlignment" Value="Right"/>
                                            <Setter Property="VerticalAlignment" Value="Center"/>
                                            <Setter Property="Margin" Value="0,0,24,0"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>

                                <DataGridTextColumn Header="{DynamicResource Col_Note}" Binding="{Binding Napomena}" Width="220">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Foreground" Value="#64748B"/>
                                            <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
                                            <Setter Property="ToolTip" Value="{Binding Napomena}"/>
                                            <Setter Property="VerticalAlignment" Value="Center"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>

                                <DataGridTextColumn Header="{DynamicResource Col_Items}" Binding="{Binding Stavke}" Width="*">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Foreground" Value="#475569"/>
                                            <Setter Property="TextWrapping" Value="Wrap"/>
                                            <Setter Property="VerticalAlignment" Value="Center"/>
                                            <Setter Property="LineHeight" Value="23"/>
                                            <Setter Property="Margin" Value="0,0,20,0"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                            </DataGrid.Columns>

                            <!-- Kad nema narudžbi -->
                            <DataGrid.Style>
                                <Style TargetType="DataGrid">
                                    <Setter Property="ItemsSource" Value="{Binding NarudzbePrikaz, RelativeSource={RelativeSource AncestorType=Window}}"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding NarudzbePrikaz.Count, RelativeSource={RelativeSource AncestorType=Window}}" Value="0">
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate>
                                                        <Border Background="White" CornerRadius="20" BorderBrush="#E2E8F0" BorderThickness="1.5">
                                                            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Margin="60">
                                                                <TextBlock Text="Nema narudžbi za prikaz" FontSize="20" Foreground="#94A3B8" FontWeight="Medium"/>
                                                                <TextBlock Text="Probaj promijeniti filter ili period" Foreground="#CBD5E1" FontSize="14" Margin="0,12,0,0"/>
                                                            </StackPanel>
                                                        </Border>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </DataGrid.Style>

                        </DataGrid>
                    </ScrollViewer>
                </Border>

                <!-- Zarada -->
                <TextBlock x:Name="ZaradaText"
                           Grid.Row="2"
                           FontSize="16"
                           FontWeight="Bold"
                           Foreground="{DynamicResource SuccessBrush}"
                           HorizontalAlignment="Right"
                           Margin="0,16,20,0"
                           Text="Ukupna zarada: 0,00 KM"/>
            </Grid>

            <!-- Proizvodi -->
            <Grid x:Name="ProductsPanel" Grid.Row="1" Visibility="Collapsed" Margin="0,20,0,0">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                        <Button Content="{DynamicResource Btn_AddProduct}" Click="DodajProizvod_Click"
                                Width="160" Height="40" Margin="0,0,10,0"
                                Background="{DynamicResource AccentBrush}"
                                Foreground="White" FontWeight="Medium"
                                BorderThickness="0" Cursor="Hand">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}" CornerRadius="10" Padding="12,0">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="{DynamicResource AccentHover}"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                        <Button Content="{DynamicResource Btn_EditProduct}" Click="UrediProizvod_Click"
                                Width="160" Height="40"
                                Background="#F39C12" Foreground="White" FontWeight="Medium"
                                BorderThickness="0" Cursor="Hand">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}" CornerRadius="10" Padding="12,0">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#E67E22"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                    </StackPanel>
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <ItemsControl x:Name="ProductsList">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Margin="10" CornerRadius="10"
                                            Background="{DynamicResource PanelBackground}"
                                            BorderBrush="#E0E0E0"
                                            BorderThickness="1"
                                            Width="200" Padding="12">
                                        <StackPanel>
                                            <TextBlock Text="{Binding Naziv}"
                                                       FontWeight="Bold"
                                                       FontSize="14"
                                                       Foreground="{DynamicResource TextBrush}"/>
                                            <TextBlock Text="{Binding KategorijaNaziv}"
                                                       FontStyle="Italic"
                                                       FontSize="12"
                                                       Foreground="{DynamicResource TextBrush}"
                                                       Opacity="0.7"/>
                                            <TextBlock Text="{Binding Cijena, StringFormat={}{0:F2} KM}"
                                                       FontSize="12"
                                                       Foreground="{DynamicResource TextBrush}"/>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </StackPanel>
            </Grid>
        </Grid>
    </Grid>
</Window>